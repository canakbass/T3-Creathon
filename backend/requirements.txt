fastapi>=0.110.0
uvicorn[standard]>=0.28.0
sqlalchemy>=2.0.0
pyjwt>=2.8.0
bcrypt>=4.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
email-validator>=2.0.0
pytest>=8.0.0
httpx>=0.27.0

# Hasan'in ai-doc-analysis modulu icin (app/services/ai.py analyze_document)
pdfplumber
langdetect

# --- Supabase (ISTEGE BAGLI) ---
# Bunlar kurulu OLMASA da sistem calisir: DATABASE_URL yoksa SQLite,
# SUPABASE_URL yoksa yerel disk kullaniliyor.
#
# Postgres surucusu. Python 3.14 icin hazir tekerlek (wheel) mevcut.
psycopg2-binary>=2.9.12
# Supabase Storage istemcisi (storage3'u de getiriyor).
supabase>=2.31.0
